// Code generated by Wire protocol buffer compiler, do not edit.
// Source: runtime.v1alpha2.Device in io/k8s/cri_api.proto
package runtime.v1alpha2

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.internal.sanitize
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.hashCode
import kotlin.jvm.JvmField
import okio.ByteString

/**
 * Device specifies a host device to mount into a container.
 */
class Device(
  /**
   * Path of the device within the container.
   */
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "containerPath"
  )
  val container_path: String = "",
  /**
   * Path of the device on the host.
   */
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "hostPath"
  )
  val host_path: String = "",
  /**
   * Cgroups permissions of the device, candidates are one or more of
   * * r - allows container to read from the specified device.
   * * w - allows container to write to the specified device.
   * * m - allows container to create device files that do not yet exist.
   */
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY
  )
  val permissions: String = "",
  unknownFields: ByteString = ByteString.EMPTY
) : Message<Device, Nothing>(ADAPTER, unknownFields) {
  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN
  )
  override fun newBuilder(): Nothing = throw AssertionError()

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is Device) return false
    if (unknownFields != other.unknownFields) return false
    if (container_path != other.container_path) return false
    if (host_path != other.host_path) return false
    if (permissions != other.permissions) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + container_path.hashCode()
      result = result * 37 + host_path.hashCode()
      result = result * 37 + permissions.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """container_path=${sanitize(container_path)}"""
    result += """host_path=${sanitize(host_path)}"""
    result += """permissions=${sanitize(permissions)}"""
    return result.joinToString(prefix = "Device{", separator = ", ", postfix = "}")
  }

  fun copy(
    container_path: String = this.container_path,
    host_path: String = this.host_path,
    permissions: String = this.permissions,
    unknownFields: ByteString = this.unknownFields
  ): Device = Device(container_path, host_path, permissions, unknownFields)

  companion object {
    @JvmField
    val ADAPTER: ProtoAdapter<Device> = object : ProtoAdapter<Device>(
      FieldEncoding.LENGTH_DELIMITED, 
      Device::class, 
      "type.googleapis.com/runtime.v1alpha2.Device", 
      PROTO_3, 
      null
    ) {
      override fun encodedSize(value: Device): Int {
        var size = value.unknownFields.size
        if (value.container_path != "") size += ProtoAdapter.STRING.encodedSizeWithTag(1,
            value.container_path)
        if (value.host_path != "") size += ProtoAdapter.STRING.encodedSizeWithTag(2,
            value.host_path)
        if (value.permissions != "") size += ProtoAdapter.STRING.encodedSizeWithTag(3,
            value.permissions)
        return size
      }

      override fun encode(writer: ProtoWriter, value: Device) {
        if (value.container_path != "") ProtoAdapter.STRING.encodeWithTag(writer, 1,
            value.container_path)
        if (value.host_path != "") ProtoAdapter.STRING.encodeWithTag(writer, 2, value.host_path)
        if (value.permissions != "") ProtoAdapter.STRING.encodeWithTag(writer, 3, value.permissions)
        writer.writeBytes(value.unknownFields)
      }

      override fun decode(reader: ProtoReader): Device {
        var container_path: String = ""
        var host_path: String = ""
        var permissions: String = ""
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> container_path = ProtoAdapter.STRING.decode(reader)
            2 -> host_path = ProtoAdapter.STRING.decode(reader)
            3 -> permissions = ProtoAdapter.STRING.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return Device(
          container_path = container_path,
          host_path = host_path,
          permissions = permissions,
          unknownFields = unknownFields
        )
      }

      override fun redact(value: Device): Device = value.copy(
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }
}
